# 琵琶湖 湖底図と可視化 — まとめ

2026-08-29 時点。作業の経緯・失敗した試み・差し替え前の数値は
`HANDOFF_琵琶湖_湖底図.md` と `HANDOFF_琵琶湖引き継ぎ1.md` §13・§19 を参照。
本書は**確定した内容と、使うときに守ること**だけ。

モデル本体（パラメータ・較正・主結果）は `琵琶湖_成果まとめ.md`。

---

## 1. 湖底図 — 何をどう作ったか

**実測のDEMは入手できていない。公開図と公表統計値から復元したもの。**

1次元モデルに必要なのは平面形ではなく **A(z)＝深さ z における水平面積**
（面積深度曲線）である。「どの深さにどれだけ水があるか」が分かれば
1次元モデルは成立する。フル地形が要るのは2D/3Dに戻る場合と、
可視化で湖の形を見せる場合だけ。

### 1.1 面積深度曲線 A(z)

**唯一の情報源は `sym/hypsography.py`。** 実測5点を通す単調内挿。

| 深度    | A [km²] | 出典                 |
| ----- | ------- | ------------------ |
| 0     | 623.0   | 公表値（北湖の面積）         |
| 40    | 346.5   | 色分け図の帯             |
| 60    | 223.5   | 同                  |
| 80    | 80.9    | 同                  |
| 90    | 24.0    | 等深線図の実測 + 色分け図との折衷 |
| 104.1 | 0.0     | 公表値（最大水深）          |

体積 27.3 km³ を追加拘束とし、全拘束を 0.03σ 以内で満たす
（体積 27.30 km³ / 平均水深 43.8 m / 単調減少）。

**2パラメータ冪関数は廃止した。** 上の5点を表現できず、A(40)/A(60) を
6〜13% 外し、A(90) を実測の約1.7倍に外挿していた。
データではなく関数形が限界だった。

### 1.2 概略湖底図 `sym/biwa_bathymetry.npz`

| 中身                | 内容                                            |
| ----------------- | --------------------------------------------- |
| `depth`           | 水深 [m]、386×518 格子、約150m解像度                    |
| `lat` / `lon`     | 各格子点の緯度・経度（N35.115〜35.516 / E135.897〜136.266） |
| `mask`            | 湖の内外                                          |
| `north` / `south` | 北湖・南湖（くびれ y=349 で自動分離）                        |
| `px_km2`          | 1画素の面積 0.0207273 km²                          |

**作り方**: 色分け図から湖岸線と5つの深度帯を抽出 → 帯番号と帯内の相対距離で
「深さの順位」を作る → 順位を A(z) で深度に変換。
**この3段目により、生成地形の面積深度関係は A(z) に厳密に一致する。**

検算（北湖）: 面積 623.00 km² / 体積 27.30 km³ / 平均水深 43.82 m /
最大 104.08 m。南湖は 60.79 km² / 平均 3.17 m / 最大 8.00 m。

### 1.3 保証されること・されないこと

|            |                                                               |
| ---------- | ------------------------------------------------------------- |
| **保証される**  | 体積・平均水深・各深度の面積、**40/60/80/90m 等深線の位置**、深部が北湖の西寄りにあるという大局的分布   |
| **保証されない** | **個々の地点の水深**（帯の内部は内挿）。とくに **0〜40m は実測点が1つも無く、形は体積拘束だけが決めている** |
| **入っていない** | **島**（沖島・竹生島）。150m解像度の色分け図から起こした都合で落ちている                      |

論文・発表で使うときは「概略地形」と明記し、生成方法を書くこと。
**個別地点の水深を主張してはいけない。**

### 1.4 CSV での取り出し

`sym/bathy_to_csv.py` → `biwa_bathymetry_shore.csv`（34,634行）

```
row,col,lat,lon,x_m,y_m,depth_m,kind,basin
50,260,35.517568,136.147059,6913.1,26378.8,0.000,shore,north
51,261,35.516216,136.148693,7061.6,26228.4,0.010,lake,north
```

- `x_m`/`y_m` は **USD・Blender シーンと同じ投影規約**
  （原点 N35.2806 E136.0710、x=東[m]、y=北[m]）。そのまま重なる
- `kind` で `lake`（32,990）と `shore`（1,644）を分離できる
- **湖岸リングは mask の外側に接する陸セルを depth=0 で追加したもの。**
  最外周の湖内セルは深度 0〜58.5m（西岸が急峻）あり、そこを0で潰すと
  地形が壊れる。リングは南北を合わせた mask 全体に対して作るので、
  **くびれ（北湖/南湖の境）に偽の岸はできない**
- `depth_m` は**下向き正**。3Dに置くときは `z = -depth_m`

### 1.5 A(z) の精度はモデル結果を動かさない

角柱 → A(z) 導入で全層循環は15日早まるが、**A(z) の中身を実測で
作り直しても到達日は1.5日しか動かない**。体積が拘束されているため、
深層を増やせば表層が必ず減って相殺する。

**A(z) を比較するときは面積の比ではなく体積の分配を見ること。**
深部の面積比（60〜80mで1.28〜1.42倍）だけを見て「遅くなる」と判断すると外す。

### 1.6 残っている律速

1. **`g8.jpg` の高解像度版**（目安 1500×2000px）。現状 500×639px では
   90m等深線しか取れない。低解像度のままのアルゴリズム工夫は2手とも失敗した
2. **0〜40m に実測点を1つ**。20m の等深線が1本取れれば決着する
3. 国土地理院 `lake1` タイル（z16, 2m/px, 4453枚取得済み）からの自動抽出は
   **部分的失敗**。A(60) が 24.7（真値223.5）と桁で外れる。主曲線の
   取りこぼしが原因
4. **未検証**: 「北湖に盆地が2つある」（近江舞子沖 約80m / 今津沖 90m超）を
   自作地形が再現しているか

---

## 2. 可視化 — 環境と役割分担

**すべて FMV で完結する。Spark に送る必要はない。**

|                                | 用途                          | 注意                                 |
| ------------------------------ | --------------------------- | ---------------------------------- |
| **ParaView 6.2.0**（+VTK 9.7.0） | `.vtp`/`.pvd`、スカラー切替、状態ファイル | `pvpython` に numpy あり。`pxr` は無い    |
| **Blender 5.0**                | USD → 動画                    | **FFmpeg 出力を持たない**。PNG連番＋外部 ffmpeg |
| **usd-core 26.8**              | USD を API で生成し読み戻して検証       | usdview は無い                        |

`HANDOFF_琵琶湖_湖底図.md` §4.6 の「FMV には ParaView も vtk も無い」は
**2026-08-29 に誤りと判明**した（§19.1）。

---

## 3. 「流体として動かす」をどう成立させたか

§13 は「**速度ベクトルは出力していない。モデルは流れを解かずパラメータ化
しているため、出せば嘘になる**」と決めている。この制約を守ったまま
流体的な動きを出すために、**拡散のラグランジュ表現**を使う。

1次元モデルが解いているのは

```
dT/dt = d/dz ( K(z,t) dT/dz )
```

であり、これとラグランジュ的に等価なのが、局所 K に応じたランダムウォーク

```
dz = (dK/dz) dt + sqrt(2K) dW        （伊藤形）
```

で動く粒子群である。**粒子の動きはモデルが解いている式そのもの**なので
飾りではない。**水平方向には動かさない**（1次元モデルに水平流は無い）。
粒子の水平位置は固定で、動くのは深さだけ。

### 3.1 数値スキームの要件（守らないと嘘になる）

| 要件                             | 理由                                                         |         |         |
| ------------------------------ | ---------------------------------------------------------- | ------- | ------- |
| **Visser の中点スキーム**             | 素朴な形は K が急変する混合層底で破綻する                                     |         |         |
| **K を鉛直に σ=3m で平滑化**           | K は1格子で3桁跳ぶ（1.3e-2 → 1.6e-5）実質不連続。平滑化しないと dt をいくら下げても合格しない |         |         |
| **dt ≤ 約60秒**（`--sub 1440` 以上） | 実測で決めた値。解析式 `1/max                                         | d²K/dz² | ` は甘すぎる |
| **K と dK/dz は同じ折れ線から作る**       | 界面値をそのまま `np.interp` すると端で「平らなのに勾配がある」矛盾した場ができる            |         |         |
| **K_bed があるときは K に加える**        | DO 版では境界混合が入る。落とすと粒子が深層で動かず DO の絵と矛盾する                     |         |         |

平滑化は**粒子の運動にしか効かない**。表示する水温はオイラー的な
有限体積解そのままで影響を受けない。

### 3.2 検証は2本立て（`sym/`）

| スクリプト                    | 何を確かめるか                        |
| ------------------------ | ------------------------------ |
| **`test_wellmixed.py`**  | K を凍結したとき一様分布が一様のまま保たれるか（必要条件） |
| **`check_particles.py`** | 粒子の深度分布が水の体積分布と一致するか           |

後者は全フレームで **±0.006 以内**に一致することを確認済み。

**レンダリングの見た目では判断できない。** 粒子が表層に密集して見えたのは
湖底メッシュが内部を隠していたためで、分布自体は正しかった。

---

## 4. 出せる量と、その意味

| 量                                  | 出典          | 意味                     |
| ---------------------------------- | ----------- | ---------------------- |
| `temperature_C`                    | 1次元カラム解     | 成層と全層循環                |
| `DO_mgL`                           | 通年DO解       | 貧酸素化。**未較正・範囲でしか出せない** |
| **`age_days`**                     | **粒子の履歴**   | 最後に表層(5m)に触れてからの日数     |
| `depth_m`                          | —           | 深度                     |
| `K_eff` `N2` `w_star` `ventilated` | `out_vtk2/` | 混合の強さ・成層度・対流速度スケール・換気  |

### 4.1 滞留日数は粒子でしか出せない

オイラー的な1次元解は「この水がいつ大気に触れたか」を保持しない。
粒子なら持ち回れる。貧酸素化は「深層水が何日ぶんの消費を溜め込んだか」
なので、DO と並べると **貧酸素な水 ＝ 長く表層に触れていない水** として
境界が一致することが一目で分かる。

§13 が「速度の代わりに湖底の換気履歴を出す」としていたものを、
水塊ごとに持たせた形である。

| 日付             | DO [mg/L]          | 滞留日数      | 混合層   |
| -------------- | ------------------ | --------- | ----- |
| 03/01（起点）      | 11.99 一様           | 0         | 全層混合  |
| 08/28          | 4.06〜10.20         | 〜181日     | 4.8m  |
| **12/01**      | **2.46〜10.22**     | **〜275日** | 31.8m |
| **03/01（1年後）** | **11.95〜12.02 一様** | **〜32日**  | 全層循環  |

### 4.2 DO を見せるときの注意

- **SOD/R_w は範囲でしか出せない**（SOD 500〜1000 / R_w 0.0025〜0.010）。
  単一値を確定値として出さない。可視化も両端を用意してある
- スカラーバーの題は `dissolved oxygen (UNCALIBRATED)` にしてある
- **DO ソルバは2つある。新規は必ず `calib.solve_oxygen_annual` を使う**
  （`fd_oxygen.solve_to` は定数SOD・境界混合なしの旧版。
  90m DO 最小の時期が 02/10 と 12/02 で変わる）

---

## 5. データと成果物

### 5.1 ParaView 用（`sym/`）

| ディレクトリ            | 中身             | スカラー               | 起点        |
| ----------------- | -------------- | ------------------ | --------- |
| **`out_do_low/`** | DO 弱い消費（推奨）    | 温度/DO/滞留日数/深度      | 3/1・400日  |
| `out_do_high/`    | DO 強い消費        | 同上                 | 3/1・400日  |
| `out_conv/`       | 温度のみ・断面        | 温度/深度              | 10/1・240日 |
| `out_conv_full/`  | 温度のみ・湖全体       | 同上                 | 10/1・240日 |
| `out_vtk2/`       | 3Dボリューム（粒子でない） | `K_eff` `N2` ほか7配列 | 10/1      |

**起点は各ディレクトリの `meta.json`。** `.pvd` は経過日数しか持たないので、
表示側で起点を決め打ちすると日付がずれる。

### 5.2 状態ファイル（開くだけで見える）

`biwa_do_low.pvsm` / `biwa_do_high.pvsm` / `biwa_conv.pvsm` /
`biwa_conv_full.pvsm`。**File → Load State…**（`File → Open` ではない）。

再生成は `pvpython make_pv_state.py`。

### 5.3 動画（Vault `biwa/`）

**通年版（3月1日起点400日）。同じ粒子・同じ期間・同じ視点なので並べて比較できる。**
各10秒 / 120フレーム / 1284×782 / ParaView・断面（東西±3km）・鉛直誇張200倍。

| ファイル | 色が表すもの | レンジ |
|---|---|---|
| **`biwa_annual_temperature.mp4`** | 水温 | 6〜22 ℃ |
| **`biwa_annual_DO.mp4`** | 溶存酸素 | 0〜12 mg/L |
| **`biwa_annual_age.mp4`** | 滞留日数 | 0〜300 日 |

見せる順は **温度 → 滞留日数 → DO**。春夏に成層が立ち上がり冬に混合層が
降りる（温度）→ 深層水が9ヶ月表層に触れていない（滞留日数）→ その古い水が
酸素を使い切っている（DO）、と話が繋がる。

**従来版（10月1日起点240日・温度のみ）。夏の成層が入っていない。**

| ファイル | 描画 |
|---|---|
| `biwa_overturn.mp4` | Blender・断面（10秒） |
| `biwa_overturn_slow.mp4` | 同・60秒3周（展示用） |
| `biwa_overturn_pv.mp4` | ParaView・断面 |
| `biwa_overturn_full.mp4` | Blender・湖全体 |

### 5.3.1 静止画（Vault `biwa/`）

| ファイル | 内容 |
|---|---|
| `do_1201.png` / `age_1201.png` | **通年版**の12月1日（最も貧酸素）。DO と滞留日数で境界が一致する対 |
| `do_0109.png` / `age_0109.png` | 旧DOソルバ時代の1月9日。**参考**（§19.19 で作り直し前） |
| `overturn_1130.png` / `_0130.png` / `_0219.png` | Blender・断面・温度 |
| `pv_overturn_1130.png` / `_0129.png` | ParaView・断面・温度 |
| `full_1130.png` / `full_0219.png` | Blender・湖全体・温度 |
| **`fig_temp_depth_time.png`** | **深度-時間断面（全層循環の主図）** |
| `fig_temp_depth_time_OLD.png` | 較正前の同図。**論文・発表に使わないこと**（全層循環が1ヶ月早い） |
| `fig_do_range_age.png` | DOの範囲（両端）と滞留日数の時系列 |
| `biwa_csv_preview.png` | 湖底図CSVの確認図 |

### 5.3.2 中間生成物（`sym/`、再生成できる）

| | 内容 |
|---|---|
| `pv_temperature_C/` `pv_DO_mgL/` `pv_age_days/` | 通年版のPNG連番＋mp4 |
| `pv_shots/` `conv_shots/` `full_shots/` | 従来版のPNG連番 |
| `blender_shots/` | §13時代の水温アニメ（較正前） |
| `*.usdc` `*.blend` | 粒子・シーン |

### 5.4 スクリプト（`sym/`）

| ファイル                                       | 役割                                                     |
| ------------------------------------------ | ------------------------------------------------------ |
| `make_convection.py`                       | 粒子生成。`--ve` 誇張 / `--slab` 断面 / `--do` DO / `--npz-out` |
| `make_convection_blend.py`                 | USD → .blend（第5引数 `side` で真横視点）                        |
| `render_convection_movie.py`               | PNG連番＋ラベル焼き込み                                          |
| `npz_to_vtp.py`                            | npz → `.vtp`/`.pvd`/`meta.json`（**pvpython**）          |
| `paraview_convection.py`                   | ParaView 表示・静止画・PNG連番                                  |
| `make_pv_state.py`                         | `.pvsm` 生成（**pvpython**）                               |
| `plot_temperature.py`                      | 深度-時間断面（全層循環の主図）                                       |
| `column_to_vtk.py`                         | 3Dボリューム時系列                                             |
| `make_lake_scene.py` / `bathy_to_usd*.py`  | USD シーン                                                |
| `bathy_to_csv.py`                          | 湖底図 → CSV                                              |
| `test_wellmixed.py` / `check_particles.py` | **検証**                                                 |

pvpython は `pxr` を持たず、システム python は `vtk` を持たないので
npz を挟む:

```
make_convection.py → .npz → npz_to_vtp.py (pvpython) → .vtp/.pvd
```

---

## 6. 見せ方の作法

### ① 配色レンジは固定する
毎フレーム自動スケールにすると「差が消える」ことが画面から読めなくなり、
全層循環が伝わらない。温度 6〜22℃ / DO 0〜12 mg/L / 滞留日数 0〜300日。

### ② 鉛直誇張はデータに焼き込まない
ParaView は Transform の Scale Z で掛ける。実スケール（62km × 104m）では
1:600 でほぼ板に見える。断面なら200倍、湖全体の形なら60倍。
**粒子と湖底で値を揃えること。**

### ③ 湖底は半透明にする（alpha 0.22〜0.30）
不透明だと手前の縁が内部の粒子を隠す。粒子の8割は10m以深にいるのに
表層の帯しか見えず、分布が偏っているように誤解させる。

### ④ 鉛直構造を見せるなら断面
全層循環は鉛直現象。湖全体版は仰角を上げると平面図になって構造が消える。
`--slab` で東西±3km を切り、真横（仰角6度）から見るのが最も読める。
帯の中心は湖の中心ではなく**最深点の東西位置**に取る。

### ⑤ 図にはどのパラメータで描いたかを添える
§13 の可視化は較正前の設定で描かれていたのに、幾何形状の残差を論じる
材料に使われていた。可視化を根拠に物理を議論するなら必須。

### ⑥ 「ファイルができた」と「中身が正しい」は別
- USD は `pxr` で読み戻す
- VTK は実際に ParaView に読ませる（時系列が 0,1,2… に潰れていないか）
- `.pvsm` は **GUI で開けるか**まで確かめる（pvpython では描画できても
  レイアウト未割当だと GUI でビューが空になる）

---

## 7. Blender / ParaView で踏んだ罠

### Blender

| 罠                                            | 対処                                                  |
| -------------------------------------------- | --------------------------------------------------- |
| 初期視距離は約17m。この湖は60km                          | `region_3d.view_distance` を設定（`make_blend.py` 対応済み） |
| カメラの `clip_end` 既定は100m。60km先は**完全に消えて真っ灰色** | `1e7` にする。ビューポートの `clip_start` は 10m へ              |
| その `clip_start=10m` が**オーバーレイを消す**           | テキストは `clip_start*5` の距離に置く                         |
| **FFmpeg 出力を持たない**                           | PNG連番＋外部 ffmpeg                                     |
| 既定フォントに**日本語グリフが無い**                         | ラベルは ASCII（`12/01`）。JSON は UTF-8 で書く                |
| 注視点を湖底 bbox に取ると**南湖の尾に引っ張られて構図が偏る**         | 注視点は粒子の bbox に合わせる                                  |

### ParaView

| 罠                                                                | 対処                               |
| ---------------------------------------------------------------- | -------------------------------- |
| `.pvd` は**XML形式しか参照できない**。レガシー `.vtk` を並べると時系列にならず 0,1,2… の連番になる | `.vti`/`.vtp` を使う                |
| appended raw は XML パーサを壊す（小さなテストでは通る）                            | インライン base64、または VTK 本体のライターに任せる |
| 湖底を構造格子で書くと**湖外まで描かれる**                                          | 湖内の四角形だけを面にする（POLYDATA）          |
| `ResetCamera()` をカメラ設定の**後**に呼ぶと向きが上書きされる                        | 向きを決めてから呼ぶ                       |
| プリセット名は `Inferno` / `Viridis`（`(matplotlib)` 付きは**存在しない**）       | —                                |
| `.pvsm` を File → Open で開くとリーダー選択が出る                              | **File → Load State…**           |

---

## 8. 残っている課題（可視化側）

1. **展示向けの作り込みは未着手。** 今あるのは検証用の絵
2. **Omniverse Kit CAE は【保留】。** 着手しないこと。理由と再開条件は §9
3. **湖全体版は鉛直構造が読みにくい。** 断面版のほうが上
4. **モデル側が Q_0 を解き直すと全部作り直し。** ただし較正値は
   `calib.py` に一本化してあるので、スクリプトを叩き直すだけで追従する
5. 湖底図の律速2つ（§1.6）は未解決

---

## 9. Omniverse Kit CAE — 【保留】着手しないこと（2026-08-29 判断）

ロードマップ トラックC の最終形として想定されていたが、**現時点では
着手できないし、着手する価値も薄い。** 次に読む人が試みて詰まらないよう、
判断ごと残す。

### 9.1 ハード的に不可

| 端末 | 可否 |
|---|---|
| **FMV**（開発機） | **原理的に不可**。Intel Arc で CUDA 非対応、RTX が要る Kit CAE は動かない |
| **A6000** | 第一候補だが**現在使用できない** |
| **DGX Spark**（GB10） | **可能性のみ・未検証**。Omniverse Kit が動く実績は Isaac Sim であるが、Kit CAE 自体は試していない |

### 9.2 機能の大半がこの案件では効かない

| Kit CAE の売り | この案件で |
|---|---|
| **流線・ベクトル場** | **✗ 使えない**。速度場を出さない方針（出せば嘘）。看板機能が丸ごと不要 |
| RTX ボリュームレンダリング | △ 1次元モデルなので水温は水平一様。豪華に塗っても情報が増えない |
| 巨大データの高速描画 | ✗ 不要。現データは 17MB。ParaView が瞬時に捌く |
| 等値面 | △ ParaView でもできる |
| USD ネイティブ | ○ 既に `.usdc` で出している |
| RTX の絵作り | ○ 展示品質の見栄えは上がる |
| **Kit App Streaming** | **◎ ここだけが本命**。GPU 無しの端末・ブラウザから操作できる |

### 9.3 【本質】サロゲートが先

Kit CAE を入れる理由は実質「ブラウザ配信」1点だが、ロードマップが想定する
**「スライダーで即更新」にはサロゲート（トラックB）が要る。未着手である。**

計算は1回 数分〜十数分かかる。サロゲート無しで Kit CAE に載せても
**再生と視点操作しかできず、それは動画とほぼ同じ**で、配信の手間に見合わない。

**順番: サロゲート（B） → Kit CAE（C）。** 逆にすると価値が出ない。
したがって A6000 が空くのを待つ間に失うものは実質ない。

### 9.4 当面の代替 —— ParaView の配布

**教育・展示の目的は、既にあるもので達成できる。**

| やりたいこと | 手段 |
|---|---|
| 深呼吸を見せる | `biwa_annual_temperature/DO/age.mp4` の3本 |
| 温暖化で起きなくなることを見せる | `biwa_fail_DO/age.mp4` と `fig_overturn_fail.png` |
| **学生に操作させる** | **`.pvsm` ＋ `out_do_low/` を配って ParaView** |

ParaView は**無料・全OS・GPU不要**で、学生の端末に入れられる。
時刻・視点・スカラー（温度/DO/滞留日数）をすべて操作できる。
データ一式 17MB なので USB でも配れる。**配信サーバが要らない分、
Kit App Streaming より運用が軽い。**

Kit CAE が優位に立つのは「学生の端末に何もインストールさせたくない」
場合だけである。授業でその制約が本当にあるかを先に確かめること。

### 9.5 再開の条件

以下が揃ってから着手する。

1. **A6000 が使える**（または Spark で Kit CAE が動くことを確認）
2. **サロゲート（トラックB）が動いている**
3. 「インストールさせられない」という運用要件が実在する

当たりを取るなら、A6000 で `out_conv/` を読ませてみるのが最小。半日程度。
