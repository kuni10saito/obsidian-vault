---
tags: [SO-101, GR00T, 実行計画, Robometer, ステップ0完了]
created: 2026-08-16
---

# ステップ0完了後の具体作業

[[【まとめ】いまの作業の意味づけと次の工程]]／[[【まとめ】次の工程 その先]] のロードマップを、**ステップ0の実測結果**に基づいて具体化した作業ノート。

---

## 現在地

| 項目 | 結果 |
|---|---|
| c0モデル成功率 | **80%以上**（実用レベル到達） |
| 残る失敗 | **グリッパーがオブジェクトを押しのけることがある** |
| ステップ1（HIL-SERL） | **不要**。本の条件「80%未満なら」を満たさなかった |

---

## 1. 最優先：`[AUTO-GRIP]` の切り分け（無料で直る可能性）

`Desktop/run_groot.py` のコードを調査した結果、**押しのけの有力な原因はクライアント側ヒューリスティクス**と判明。

### メカニズム（539〜547行目 + 533〜534行目）

```python
arm_move = |exec_cmd - last_cmd| の M1〜M5 合計
if arm_move < GRIP_SETTLE_THRESH(40):
    grip_approach_count += 1
    → 60フレーム連続で GRIP_FORCE_TARGET(1800) に強制クローズ
```

- `LOOP_HZ = 30`、掴む前は `current_skip = 1` → **動きが小さい状態が2.0秒続くと強制クローズ**
- 問題：**物体直前の「ゆっくり微調整」フェーズこそ arm_move が小さい**
- モデルが慎重に寄せている最中にタイマー満了 → 閉じたまま前進 → **押しのけ**

### カスケード（さらに悪化する）
強制クローズ → `grip_pos < 2500` → 440行目の「掴み検出」が誤発火 → `gripping = True` → `EXEC_SKIP = 2` で**2倍速に加速** → 何も掴んでいないのに倍速で動き出す。

> 512行目のコメント「掴む前は1倍速（**オブジェクトを弾かない**）」から、過去に同種の弾き問題をEXEC_SKIPで潰した形跡あり。今回はタイムアウト側が残っていた。

### 切り分け方法（テスト1回で確定）
押しのけの瞬間、コンソールに以下が出るか確認するだけ：

```
[AUTO-GRIP] アーム停滞60f → 強制クローズ
```

| 出た | → **ヒューリスティクス問題**。データ収集も再学習も不要 |
| 出ない | → モデルの軌道が悪い。その状況限定でDAgger介入 |

### 修正案（出た場合、推奨順）
1. **`GRIP_FORCE_FRAMES: 60 → 120`**（2秒→4秒）最も安全
2. **`GRIP_SETTLE_THRESH: 40 → 20`** 「停滞」判定を厳しく
3. タイムアウト無効化は**非推奨**（本当に固まった時の救済策。外すとハング）

> **重要**：ヒューリスティクス起因なら、データを何本追加しても直らない。**データ収集の前に必ずこの切り分けを行う**。

---

## 2. ロードマップの修正

| ステップ | 判断 | 理由 |
|---|---|---|
| 1（HIL-SERL） | **スキップ** | 80%達成済み。本の分岐条件を満たさない |
| 2（Isaac＋Cosmos） | **延期** | 「環境が変わっても壊れない保険」。今はバラつきを排除して80%に到達したばかりで、視覚バリエーションを混ぜると退行リスク |
| 3（ツール） | **一部のみ** | Robometerだけ実施。下記参照 |
| 4（π0.5） | **保留** | GPUは足りるがデータが不足（数百ep必要、現状122/31ep） |
| 5（タスク複雑化） | **次の目的地** | Stacking → Sorting |

### ステップ2を復活させる条件
- ロボットを別の部屋・別端末に移す必要が出た
- 照明条件が季節/時間帯で変わって成功率が落ちた
- デモ展示など環境を固定できない場面が発生した

### ステップ3の内訳
| ツール | 判断 |
|---|---|
| **Robometer** | **やる** |
| lerobot-eval | 飛ばす。シム上の標準ベンチマークで実機pick&placeの評価にならない |
| lerobot-rollout | 保留。ステップ1を飛ばしたので当面出番なし |

---

## 3. Robometer

### ⚠️ 訂正：カメラ不一致は検出できない

当初「手作業のカメラ分類を自動化できる」と考えたが、**誤り**。

Robometerの出力は：
- **progress head**：フレームごとのタスク進捗 `[0,1]`
- **success head**：フレームごとの成功確率

→ **「タスクがどこまで進んだか」の評価であり、カメラ位置の違いは検出しない**。角度が違っても掴めていれば高スコア。

`score_lerobot_episodes`（5次元QC）も確認したが、「Visual Clarity」は明るさ・ブレ・遮蔽の検出であって視点ズレではない。

> **v4のカメラ不一致は、どちらのツールでも自動検出できない。**

### では何に使えるか（2つ、どちらも有用）

**① 成功率の自動測定** — ステップ0の目視カウントを置き換える。ステップ5でタスクが増えると測定回数も増えるのでここが効く。

**② 失敗の発生時刻を定量化** — `progress head` はフレームごとの進捗を出す。押しのけエピソードでは進捗が上がった後に停滞/低下するはず。**`[AUTO-GRIP]` のログと突き合わせれば因果が数値で確定する**。

### インストール（Spark上、既存envには触らない）

```bash
git clone https://github.com/huggingface/lerobot ~/lerobot06
cd ~/lerobot06
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[robometer]"
# uvなら: uv sync --extra robometer
```

- `groot` conda env と `Isaac-GR00T/.venv` には**絶対に触らない**
- **`Isaac-GR00T` で `git pull` しない**（Eagleファイルが消える）

### 自分のデータ固有の設定（デフォルトでは動かない）

実データ（`so101_dataset_v2/meta/info.json`）を確認した結果：

| 設定 | デフォルト | **自分の値** |
|---|---|---|
| `image_key` | `observation.images.top` | **`observation.images.front`** |
| task文字列 | — | **`"pick up the block and place it in the cup"`** |
| `max_frames` | `8` | 要調整（8フレームでは粗すぎる可能性） |

### スクリプト

```python
from lerobot.rewards.robometer import RobometerConfig, RobometerRewardModel
from lerobot.rewards.robometer.modeling_robometer import ROBOMETER_FEATURE_PREFIX
from lerobot.rewards.robometer.processor_robometer import RobometerEncoderProcessorStep

cfg = RobometerConfig(
    pretrained_path="lerobot/Robometer-4B",
    device="cuda",
    reward_output="success",                 # 成功率measure=success / 進捗曲線=progress
    image_key="observation.images.front",    # ← デフォルトのtopではない
)
model = RobometerRewardModel.from_pretrained(cfg.pretrained_path, config=cfg)

encoder = RobometerEncoderProcessorStep(
    base_model_id=cfg.base_model_id,
    use_multi_image=cfg.use_multi_image,
    use_per_frame_progress_token=cfg.use_per_frame_progress_token,
    max_frames=cfg.max_frames,
)

TASK = "pick up the block and place it in the cup"
# frames: (T, H, W, C) uint8 をエピソードごとに用意
encoded = encoder.encode_samples([(frames, TASK)])
batch = {f"{ROBOMETER_FEATURE_PREFIX}{k}": v for k, v in encoded.items()}
reward = model.compute_reward(batch)   # shape (batch_size,)
```

### 検証（既知データで校正）
cluster0（良品・80%達成）と v4全体（カメラ混在）で回す。ただし**カメラ不一致自体は拾わない**ので差が出なくても正常。見るべきは「**成功判定が自分の目視判定と一致するか**」。

### 注意点

| 項目 | 内容 |
|---|---|
| `max_frames=8` | 軌道全体から8フレームしか渡さない。押しのけの瞬間を捉えるには粗い可能性 |
| inference-only | LeRobot統合は推論専用。Robometer自体の学習は非対応 |
| 戻り値はスカラー | 内部はフレームごとでも `compute_reward()` は最終フレームの値を返す。進捗曲線が欲しければ内部APIが必要 |
| ARM64 | GB10でQwen3-VL-4B（BF16 約8GB）。119GBあるのでVRAMは余裕だがwheelビルドで詰まる可能性 |

---

## 4. カメラ一貫性チェッカー（自作、ステップ5の前提）

Robometerが埋められないギャップ。**既製ツールではなく30行程度の自作スクリプトで足りる**：

> 各エピソードの**先頭フレームだけ**を取り出す → 画像埋め込み（CLIP等）→ クラスタリング → 外れたエピソードを弾く

手作業でやったv4分類の、そのままの自動化。

> **なぜステップ5の前提か**：ステップ5は新規データ収集を伴う。それは**v4の惨事が起きたのと同じ状況**。Robometerはこのギャップを埋めないので、収集を始める前に用意しておく必要がある。

---

## 5. 計算資源の使い分け

| マシン | 起動コスト | 担当 |
|---|---|---|
| **DGX Spark**（GB10 / 119GB） | 常時 | **学習＋Robometer**。データが所在するのでQCもここ |
| **A4500**（20GB） | 即 | ARM wheelで詰まった時の避難先 |
| **A6000**（48GB） | 起動しに行く | 20GBで詰まった時のみ |
| **H100**（80GB級） | 空き待ち | 空いた時に重い学習を流す（**待たない**） |

### 判断基準
- **QCはデータが置いてある場所でやる** → Sparkが第一候補（A4500に122ep転送するのは本末転倒）
- venv隔離すれば依存衝突は起きない → 「環境を壊さないためにA4500」という理由は消える
- 唯一の実リスクは**ARM64のwheel問題**。詰まったらA4500へ
- **学習と同時にRobometerを走らせない**（GPU OOM競合）
- A6000を起動しに行く基準：LingBot-VA等24〜32GB必要／π0.5推論がOOM／Sparkと並行学習したい

---

## 6. Robometerの後：ステップ5（Stacking）

```
1. [AUTO-GRIP] 切り分け           ← 今ここ
2. 必要ならパラメータ修正（無料）
3. Robometer導入・既知データで検証   ← Spark
4. カメラ一貫性チェッカー自作        ← ステップ5の前提
5. ステップ5：Stacking
   - 新タスクのデータ収集（collect_data.py、task文字列を変更）
   - カメラはcluster0位置を厳守（第13章の鉄則）
   - 収集のたびに一貫性チェッカーを回す
   - 既存チェックポイントから継続FT（ゼロからやり直さない）
   - Robometerで成功率を自動測定
   - 改善ループを回す
6. データが数百epに育ったら → ステップ4（π0.5）が選択肢に入る
```

> 背骨は不変：[[Clippings/第13章　推論デプロイと評価・改善ループ｜SO-101ロボットアーム 実践ガイド|第13章]]の**「収集環境と評価環境を一致させ、失敗を1つずつデータで潰す」**。

---

## 参考リンク
- [ROBOMETER · Hugging Face Docs](https://huggingface.co/docs/lerobot/en/robometer)
- [lerobot/Robometer-4B](https://huggingface.co/lerobot/Robometer-4B)
- [score_lerobot_episodes](https://huggingface.co/blog/robotdata/score-lerobot-episodes)
- [LeRobot v0.6.0: Imagine, Evaluate, Improve](https://huggingface.co/blog/lerobot-release-v060)
