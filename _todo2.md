**⑨ 地形USDの南端を直す（§4.8.8）**
`fetch_terrain.py` の `LAT0 = 35.05` が南湖の南端を切っており、
**湖セルの3.19%が欠落**している。`LAT0 = 34.94` に下げて再実行するだけ
（タイルはキャッシュ済みなので追加行のみ取得）。北湖の物理には影響しないが、
湖全体を描くと南端が直線で切れる。

**⑩ `biwa_terrain_hi.usdc` を ve=3 で作り直す**
現物は **ve=5 で作られており針山状に破綻**している（§4.7.6）。
392 MB を再生成する価値があるかは用途次第。実用は `_mid`（ve=3）で足りる。

