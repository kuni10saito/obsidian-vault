---
tags: [SO-101, IsaacLab, 強化学習, sim-to-real, 引き継ぎ, HANDOFF]
created: 2026-09-11
---

# HANDOFF：トラックB（Isaac Lab 強化学習の実機化）

セッション `d5fe2cf1`（2026-08-29〜09-11、5379発話）の引き継ぎ。
**Spark への移行前に作成。**

> **最上位** → [[SO101_現状まとめ_20260909]]
> **全記録** → [[SO101まとめ/IsaacLab_SO101_実機化]]
> **技術詳細** → [[SO101まとめ/URDFとFK_20260910]]
> **並行セッション** → [[並行セッション関係図_20260911]]

---

## 0. ★2026-09-12 の最大の発見：基準値が根本的に間違っていた

**`SERVO_AT_DEFAULT` は `HOME_POSITION` と同じではなかった。** シムと実機の手先位置が
**73mm** ずれていた。未解決だった誤差予算「1.5〜2cm」の**3〜5倍**。

### 何が起きていたか

`CLAUDE.md` の HOME_POSITION の備考欄がすべてを説明する。

| モーター | 値 | 備考 |
|---|---|---|
| M2 shoulder_lift | 2242 | **「+10度が最適」** |
| M3 elbow_flex | 1400 | **「アプローチ感」** |
| M4 wrist_flex | 2375 | **「+30度」** |

**HOME値は GR00T の模倣学習タスク用に「感覚で」決めたもの**だった。
ところがコードは `SERVO_AT_DEFAULT = dict(HOME_POSITION)` として、
**それがシムのデフォルト姿勢と一致すると仮定**していた。根拠はなかった。

### 測定方法（定規だけ。アームを動かさずにできる）

URDFから各関節軸の高さを予測し、テーブル面から定規で実測して突き合わせる。

| 測定点 | 物理的な目印 |
|---|---|
| `shoulder_pan` 軸 | ベース回転部の中心（モーター上部あたり） |
| `shoulder_lift` 軸 | 2番目のモーターの回転中心 |
| `elbow_flex` 軸 | 3番目のモーターの回転中心 |
| `wrist_flex` 軸 | 4番目のモーターの回転中心 |
| `wrist_roll` 軸 | M6グリッパーモーターの根元 |
| 爪の掴む点 | `gripper_frame_link`（2本の爪の間、先端寄り） |

**根元から順に測ると、どこで誤差が入るかが特定できる。**

### 実測結果（2026-09-12）

姿勢1 `raw=[1510,2231,1503,2365,2437]`

| 測定点 | 実測 | 従来モデルの予測 | 差 |
|---|---:|---:|---:|
| pan軸 | 63 | 62.4 | +0.6 ✅ |
| lift軸 | 117 | 116.6 | +0.4 ✅ |
| elbow軸 | 235 | 229.6 | +5.4 ✅ |
| **wrist_flex軸** | **155** | 215.8 | **−61** ❌ |
| wrist_roll軸 | 110 | 155.4 | −45 ❌ |
| 爪 | 10 | 59.7 | −50 ❌ |

**肘軸までは完全に正常。その先が一斉に45〜60mm低い。**

姿勢2 `raw=[1496,2235,1243,2365,2437]`（M3のみ移動）

| 測定点 | 実測 |
|---|---:|
| wrist_flex軸 | 212 |
| 爪 | 80 |

### 当てはめ結果

| 仮説 | SAD[2] | SAD[3] | SAD[4] | RMS残差 |
|---|---:|---:|---:|---:|
| **A** | 2242（従来のまま） | **1125** | **1815** | 4.8 mm |
| **B** | **2362** | **990** | **1800** | 4.0 mm |

**3パラメータにしてもRMSは4.8→4.0とわずかしか改善しないのに SAD[3] が135カウント動く。
測定が2姿勢しかなく、しかも肘しか変えていないため、肩と手首が分離できていない。**

### ⬜ 未決：上腕の傾きを1回測れば確定する

上腕（肩→肘）の傾きは**肩の角度だけ**で決まり、1度 = 11.38カウントの線形関係。

| 仮説 | 現在の姿勢での上腕の傾き |
|---|---:|
| A | **76.6°** |
| B | **87.2°**（ほぼ鉛直） |

**10.5度差。スマートフォンの傾斜計や分度器で明確に区別できる。**

```
SERVO_AT_DEFAULT[2] = 2235 + (測定角度 − 76.0) × 11.38
```

> ⚠️ **この測定が済むまでコードへ反映しない。** SAD[2]/[3]/[4] は連動しており、
> 片方だけ入れると誤った値が残る。

### 測れなかったもの

| 関節 | 理由 | 代替案 |
|---|---|---|
| M1 pan | 鉛直軸回転なので高さが変わらない | カメラ較正（ホモグラフィ）で爪の真下の座標を測る |
| M5 wrist_roll | ロール軸回転なので位置がほぼ動かない | グリッパーの爪の向き（開閉面）で判定 |

**M2を高さで測るのは不可能**だった。上腕を水平に近づけると、
どの関節の組み合わせでもアームがテーブル面より下に入る（探索で確認済み）。

### 副次的な発見：肘のたわみは負荷依存

| 姿勢 | 指令との差 |
|---|---:|
| HOME付近 | 103カウント |
| 伸ばした姿勢（M3を1087へ指令） | **156カウント**（1243で停止） |

**定数オフセットではない。** トルク不足による負荷依存の誤差で、
固定値の補正では吸収できない。シムの `effort_limit_sim = 0.6` は
「ストールの1/3」という当て推量であり、実測していない。

---

## 1. いちばん重要なこと

**コードに未反映の実測値が3件ある。これを入れないと再現できない。**

| # | 発見 | 現在のコード | 実測値 |
|---|---|---|---|
| 1 | `SIGN[1]` shoulder_pan | `+1` | **−1** ❌ |
| 2 | `SIGN[4]` wrist_flex | `+1` | **−1** ❌ |
| 3 | グリッパー開閉値 | 開3426／閉3100 | **開3592／把持2346／全閉1854** |

加えて **`lift_mirror.py`（Spark側）は `SIGN` を通していない**。
`raw = SERVO_AT_DEFAULT + (q − DEFAULT_JOINT_POS) × CPR` と直書きしており、
符号を直してもミラーリングには反映されない。**ここも直す必要がある。**

---

## 2. 2026-09-11 の新発見：肘が自重で垂れる

HOME復帰を実行したところ、**指令値は 1400 に到達しているのに実測は 1502**。
**トルクONでも肘（M3）が102カウント＝9度垂れている。**

朝いちばんのスナップショットでも M3 は 1500 で、**再現性のある定常誤差**。

### 手先への影響

URDFのFKで計算（`urdf_fk.py`）：

```
肘の9度のたわみ単独 → 手先が X −2.56cm / Z −1.79cm、合成 3.13cm ずれる
```

**トラックBで未解決だった「把持が決まらない、誤差1.5〜2cm」と同じオーダー。**
しかもシムでは肘は垂れない（`effort_limit = 0.6` で保持できる前提）ため、
**ミラーリングでは補正されない系統誤差**。

> これは「実機のHOME姿勢＝シムのデフォルト姿勢」という
> 未検証の前提が崩れていることの、具体的な証拠になりうる。
> **次に検証すべき最有力候補。**

---

## 3. 確定した SIGN

| 関節 | SIGN | 確度 | 判定方法 |
|---|---|---|---|
| M1 shoulder_pan | **−1** | 高 | 較正データ＋FK＋カメラ画像の3点一致 |
| M2 shoulder_lift | **+1** | 中 | 口頭の向き申告（「下げた」） |
| M3 elbow_flex | **+1** | 中 | 口頭の向き申告（「上げた」） |
| M4 wrist_flex | **−1** | 高 | 実測姿勢FK＋カメラ画像＋感度検証 |
| M5 wrist_roll | **未確定** | — | 位置がほぼ動かず、姿勢で見る必要あり |
| M6 gripper | 開＝raw増加 | 高 | 全開3592／把持2346／全閉1854 |

**M2・M3 は画像方式で取り直すとよい**（`joint_check.py --joint 2` / `--joint 3`）。
口頭申告だけなので、M1・M4より確度が一段低い。

### なぜ間違えていたか

URDFの `axis` は6関節すべて `0 0 1` と書かれているが、
`origin rpy` を掛けた**実際の軸は `shoulder_pan` が `[0,0,−1]`**。
表記を読んでも分からない。→ [[SO101まとめ/URDFとFK_20260910]] §4

**追従誤差（16〜35カウント）は符号の誤りを検出できない。**
符号が逆でも実機は逆向きの指令に忠実に追従するため。指標が構造的に盲目だった。

---

## 4. 物理環境の状態（2026-09-11 移行前）

### アーム

**HOMEへ復帰済み。トルクON（姿勢保持中）。**

| 関節  |   実測 | HOME |               差 |
| --- | ---: | ---: | --------------: |
| M1  | 1510 | 1505 |              +5 |
| M2  | 2231 | 2242 |             −11 |
| M3  | 1502 | 1400 | **+102**（自重たわみ） |
| M4  | 2365 | 2375 |             −10 |
| M5  | 2437 | 2438 |              −1 |
| M6  | 3101 | 3100 |              +1 |

> 長時間の通電はサーボが発熱する。放置するなら
> `python3 go_home.py --run --torque-off-after` で切ること。
> 切ると自重で垂れる（特にM2）。

### 接続

**ロボットもカメラも DGX Spark に繋がっている。FMV には COM ポートが無い。**

| デバイス           | 用途                       |
| -------------- | ------------------------ |
| `/dev/ttyACM1` | **フォロワーアーム**             |
| `/dev/ttyACM0` | リーダーアーム                  |
| `/dev/video2`  | **テーブル俯瞰。較正済み（誤差6〜9mm）** |
| `/dev/video0`  | 正面・低位置                   |

> ⚠️ 自動検出はソート順で `ttyACM0`（リーダー）を掴む。
> **必ず `--port /dev/ttyACM1` を明示すること。**
> USB挿し直しや再起動で番号が入れ替わりうる（カメラでも同じ事故があった）。

### カメラ基準画像

移動後の復元用に、移動前の視野を記録した。

| 画像 | 内容 |
|---|---|
| ![[その他/so101_カメラ基準_俯瞰cam2_20260911.jpg]] | `/dev/video2`。較正用マスキングテープが写っている |
| ![[その他/so101_カメラ基準_正面cam0_20260911.jpg]] | `/dev/video0` |

**移動後はこの画像と見比べて位置・角度を復元すること。**
較正（ホモグラフィ）もトラックAの実機成功も、この位置に依存している。

---

## 5. Spark 上のスクリプト

| スクリプト | 用途 | **位置指令** |
|---|---|---|
| `~/go_home.py` | HOME復帰。速度6・加速度1で制限 | **あり** ⚠️ |
| `~/joint_probe.py` | 姿勢・可動域の測定（`--snapshot` `--sign` `--range` `--watch`） | なし |
| `~/joint_check.py` | 符号の画像判定（`--joint N`） | なし |
| `~/isaac_so_arm101/fk_oracle.py` | Isaac Lab版FK。**遅い・ハングする** | なし |

FMV 側（`Desktop/so101_rl/`）：

| スクリプト | 用途 |
|---|---|
| `urdf_fk.py` | **URDFから直接FK。1秒。Isaac Sim不要** |
| `so_arm101.urdf` | URDFのコピー |
| `so101_rl_deploy.py` | Reach配備（FMVからは実機に届かない） |
| `so101_lift_deploy.py` | Lift配備（同上） |
| `cube_locator.py` / `grip_calib.py` | 検出・較正 |

### Isaac Lab版FKの起動（通常は `urdf_fk.py` で足りる）

```bash
cd ~/isaac_so_arm101 && \
OMNI_KIT_ACCEPT_EULA=YES \
LD_PRELOAD=/lib/aarch64-linux-gnu/libgomp.so.1 \
PYTORCH_JIT=0 \
.venv/bin/python fk_oracle.py --joints 0,0,0,1.57,0,0 --body gripper_frame_link
```

3つの環境変数すべてが必要。1つ欠けると別々の理由で止まる。
**2026-09-10 には15分間 CPU 0% でハングした**（過去に1時間の記録あり）。

---

## 6. 次にやること（優先順）

| # | 作業 | 理由 |
|---|---|---|
| 1 | **M3の自重たわみを定量化** | 手先3.1cm。把持未達の最有力容疑 |
| 2 | **`SIGN[1]`/`SIGN[4]` をコードへ反映** | 未反映のまま。`lift_mirror.py` も要修正 |
| 3 | **グリッパー定数を実測値へ** | 開3592／把持2346。`GRIP_CLOSE` は754カウントずれ |
| 4 | M2・M3 を画像方式で再確認 | 口頭申告のみで確度が一段低い |
| 5 | HOME姿勢＝シムデフォルト姿勢 の幾何学的検証 | **未検証の前提**。1と関連 |
| 6 | M5 wrist_roll の符号 | 位置では判定不能。姿勢で見る。優先度低 |
| 7 | グリッパー開き幅の差（URDF 1252 vs 実機 1738） | シムの全開が実機では開ききっていない |

---

## 7. 安全上の決まり（守ること）

2026-08-31 に**グリッパーが外れた**。原因は
「速度は制限したが**加速度を制限していなかった**」こと。±20の瞬間反転が起きていた。

- 実機を動かす操作は**必ずユーザーの明示的な許可を取る**
- **速度と加速度の両方**を制限する（`--max-step` と `--max-accel`）
- 使用後は**必ずHOMEへ戻す**
- トルクONのサーボを手で動かさない（トルクOFFなら可）
- トルクを入れる前に**現在位置を目標値として書き込む**（飛び出し防止）

---

## 8. 移行についての注意

`~/vault-so101/` は**今日時点のコピーで、FMVのVaultと同期していない**
（琵琶湖セッションが試験用に作成）。方針は「**FMVが本番、Sparkは試験、切り替えは後日**」。

**Spark側のVaultで実作業をすると、切り替え時に差分の突き合わせが必要になる。**
試験は「起動するか・日本語が化けないか・プラグインが動くか」に留めるのが安全。

### FMV側 `CLAUDE.md` の記述が実態と食い違っている

| 記述 | 実態 |
|---|---|
| 「FMV（実行端末）＝クライアントスクリプト」 | **ロボットはSparkに接続**。FMVにCOMポート無し |
| `scp -r Desktop/so101_dataset_v3 saito@...` | 転送不要（最初からSparkにある） |
| `python Desktop/run_groot.py`（FMVから実行） | Sparkでローカル実行 |
| 接続先 `DGX Spark port 5555` | `localhost:5555` |

**未修正。** 修正するかはユーザーの判断。
`~/claude_context_fmv/CLAUDE.md`（Spark側のFMVミラー）も同様。
