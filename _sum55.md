### 5.5 地形USD（周囲の陸地＋湖底）【2026-08-30】

湖底だけだと湖が宙に浮いて見えるので、**国土地理院DEM（z=13）の陸地と
湖底を1枚の標高場に繋いだ**。詳細は `HANDOFF_琵琶湖_湖底図.md` §4.7。

| ファイル | stride | ve | 頂点 | サイズ | 用途 |
|---|---:|---:|---:|---:|---|
| `biwa_terrain.usdc` | 4 | 3 | 111万 | 22 MB | 軽量・確認用 |
| **`biwa_terrain_mid.usdc`** | **2** | **3** | **446万** | **98 MB** | **推奨** |
| `biwa_terrain_hi.usdc` | 1 | 5 | 1,783万 | 392 MB | 最高解像度（重い） |

- 陸は DEM そのまま、湖は **84.4m − 水深**。境界は湖岸線 mask で切り替わる
- 半透明の湖面（不透明度0.55）を**別プリム** `/Biwa/WaterSurface` で持つ。
  **4隅すべてが湖内の四角形だけ**を面にしている（§7 の「湖外まで描かれる」対策）
- 水平 東西63.9km × 南北67.8km、標高 −20〜1376m
- **`terrain/map.png`（6.7MB）は USD と同じフォルダに必須**

| スクリプト | 役割 |
|---|---|
| `fetch_terrain.py` | 地理院タイル → `terrain/dem.npz` / `map.png`（キャッシュ有効） |
| `make_terrain_usd.py` | DEM＋湖底 → 地形USD。`--stride` `--ve` `--no-water` `--water-alpha` |

```bash
python make_terrain_usd.py --stride 2 --ve 3    # 推奨
```

> **【出典表示が義務】**「国土地理院タイルを加工して作成」を成果物に明記すること。

**鉛直誇張は 2〜3 倍**（比較図 `biwa/fig_terrain_ve.png`）。4以上は針山になり、
5は地形として破綻する。`biwa_terrain_hi.usdc` だけ ve=5 で作られており、
**単体では不自然**。使うなら `--ve 3` で作り直すこと。

#### 既知の制約（2つ）

1. **南湖の南端が切れている。** DEM の南限が 35.0301N、湖底図は 34.9595N まで
   あるため、**湖セルの3.19%（1053/32990）が欠落**している。北湖の物理には
   影響しないが、湖全体を描くと南端が直線で切れる。
   `fetch_terrain.py` の `LAT0` を 34.94 に下げれば直る
2. **粒子USDとは鉛直方向に重ならない。** 水平原点は同じだが、地形は湖面を
   `84.4*ve` に、粒子は湖面を `0` に置く。しかも既定の ve が **3 と 200 で
   66倍違う**。重ねるなら ve を揃えたうえで粒子を `+84.4*ve` 持ち上げる

---

