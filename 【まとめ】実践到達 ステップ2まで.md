---
tags: [SO-101, GR00T, sim-to-real, 実践記録, ロードマップ, Part4]
created: 2026-08-17
---

# 実践到達 ― ステップ2まで（Part 4）

[[【まとめ】次の工程 その先]]（Part 3）までは**本の内容から工程を設計した「計画編」**だった。
この Part 4 は、2026-08-17 に**実際に手を動かして到達した結果**の記録＝「実践編」。
ステップ0〜1（改善ループ1周目）を実機で通し、**ステップ2（Isaac合成データ）の実行環境まで確証を取った**ところまでを残す。

---

## 1. これまでのあらまし

半年かけてSO-101（リーダー＋フォロワー）を組み、テレオペ→データ収集→GR00T微調整まで到達していた。
だが2026年春〜夏、**「掴めるが、その後の動作が迷走する」**という壁に長くはまっていた。

- **迷走の正体＝カメラ位置の不一致**だった。学習データ（v4）が**複数のカメラ位置を混在**して撮られており、モデルが「どの視点で見た状態か」を一貫して学べていなかった。
- 実機のライブ映像を確認すると、現物理カメラ（Camera0）は**cluster0＝俯瞰(top)視点**に一致すると判明。当初cluster1（正面）と誤認していた。
- 対策：v4から**cluster0の一貫した31エピソードだけを抽出**（`so101_dataset_v4_c0`）→ **実機データのみで再学習**（`so101_ft_n17_c0_real`、checkpoint 1500 / 3000）。

> 背骨の教訓は [[Clippings/第13章　推論デプロイと評価・改善ループ｜SO-101ロボットアーム 実践ガイド|第13章]] の鉄則そのもの ―― **「収集環境と評価環境を一致させる」**。6週間はまった原因は、この一点（視覚層のギャップ）だった。

---

## 2. ステップ0〜1の実績 ― c0モデルが実機でpick&place成功（2026-08-17）

再学習した c0（checkpoint-3000, port5556）を実機で検証：

- サーバー起動 `~/launch_c0.sh 3000` → クライアント `STREAM_PORT=8088 ~/Isaac-GR00T/.venv/bin/python ~/run_groot_n17.py`
- **1回目**：掴んだ後、横移動せず縦に上げて放り投げ（配置フェーズが退行）
- **2回目**：**掴む→運ぶ→配置が完璧に成功**。リリースはキー `o`（グリッパー強制開放）
- **決定的発見**：物体の**置き位置に「スイートスポット」**がある。学習分布（31ep）にハマる位置に置くと安定成功。外すと弾く／放り投げが出る。

### わかったこと
- **カメラ不一致という根本原因は、実機で決着**した（cluster0一貫データなら素直に掴んで運んで置ける）。
- 成功率のムラ＝**学習データが31ep/16,149frameと薄い**ことの直接的表れ。掴み（視覚的に目立つ）は学べても、**「横移動して置く」配置フェーズの頑健性が不足**。
- 位置づけ的には [[Clippings/第13章　推論デプロイと評価・改善ループ｜SO-101ロボットアーム 実践ガイド|第13章]] の改善ループ1周目を通過。本の分岐（[[Clippings/第12章　ロボット学習の主要手法とHIL-SERLの実践｜SO-101ロボットアーム 実践ガイド|第12章]]「80%未満ならHIL-SERL」）にかけるには、**まず運用レンジでの成功率ベースライン（10〜20回）**を取るのが次の小タスク。

---

## 3. ステップ2の準備完了 ― Isaac SimがDGX Sparkで動くと実証

[[Clippings/第14章　Isaac SimによるReal-to-Simシミュレーション|第14章]]／[[Clippings/第15章　Isaac SimとNVIDIA Cosmosの統合関係|第15章]] の合成データ実写化に進む前提として、**どのマシンで回すか**が課題だった。

### 論点：FMV / A6000 / Spark
- **FMVは不可**：GPUが Intel Arc 140V（CUDA非対応）。Isaac Sim・Cosmos・GR00T学習はすべてNVIDIA＋CUDA前提で、FMVは**司令塔（Claude Code＋SSH＋ロボット操作）専用**。
- **Spark(GB10)の懸念**：既知の nvrtc 問題（Isaac LabのRL環境生成が sm_121 非対応で落ちる）。

### スモークテスト結果 ＝ Sparkで描画OK
素のIsaac Sim Core API（`World`＋`DynamicCuboid`＋`Camera`）で、GB10 headless レンダリングを検証：

![[その他/isaac_smoke_spark_20260817.png]]

- **RTXカメラ描画が正常動作**（240×320×4、赤キューブ＋地面グリッドを取得）
- **nvrtcの壁を回避**（RL環境生成を通らないため発生しない）
- 起動条件：`OMNI_KIT_ACCEPT_EULA=YES` ＋ `LD_PRELOAD=/lib/aarch64-linux-gnu/libgomp.so.1`

### 結論＝切り分け
| 方式 | GB10(Spark)で動く？ | 使うマシン |
|---|---|---|
| **Real-to-Sim replay＋レンダリング**（ステップ2の本命） | ✅ 動く | **Spark（学習機と同一）** |
| Isaac LabのRLロールアウト | ❌ nvrtcで落ちる | A6000／A4500 |
| Cosmos-Transfer | （重い） | H100 |
| GR00T再学習 | ✅ | Spark |

→ **ステップ2の合成データ生成はSparkで完結できる**見込みが立った。マシン跨ぎ不要。

---

## 4. 次の一手（ステップ2の実作業）

1. **cluster0カメラをIsaac Sim内で再現**（第14章）← ここから
   - SO-101シーンをCore APIで構築 → カメラを実機Camera0（俯瞰）に合わせる
   - `Desktop\v4_camera_clusters\cluster0_31ep.png`（実機基準）と重ねて一致検証
   - **ここが合うまで先へ進まない**（合成データ全部の土台。ここを外すと同じ轍）
2. 31本の実軌道をSimで再生 → カメラ描画
3. Cosmos-Transferで照明・質感・背景のバリエーション（[[Clippings/第15章　Isaac SimとNVIDIA Cosmosの統合関係|第15章]]）
4. **sim:real = 3:7** で混合（第12.6／第15.5）
5. GR00T再学習（この時は **c0srvサーバーを停止**してGPU解放）

> 全体の位置づけは [[【まとめ】次の工程 その先]] のロードマップ（ステップ0〜9）を参照。この Part 4 は、そのうち**ステップ0〜1を実機で通過し、ステップ2の実行環境を確定した**段階にあたる。

---

## 付記：この時点の要点
- **根本原因（カメラ不一致）は解決済み**。c0はスイートスポット限定なら実用レベル。
- **残課題＝データが薄い（31ep）**。頑健化＝配置バリエーションの追加が必要で、その手段がステップ2（合成データ）。
- **実行環境が確定**：Real-to-SimはSpark、CosmosはH100、RLだけA6000/A4500。
- 迷ったら背骨に戻る：**「収集環境と評価環境を一致させ、失敗を1つずつデータで潰す」**（第13章）。
