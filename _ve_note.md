
**【例外 2026-08-30】** 地形USD（§5.5）だけは ve を頂点に焼き込んでいる。
静的な表示用アセットで、ParaView ではなく Blender / Omniverse で開くことを
想定しているため。**作り直しは `--ve` で即座にできる。**
ただしこの例外のせいで**粒子USD（既定 ve=200）と鉛直方向に重ならない**ので、
重ねるときは両方を同じ `--ve` で生成し直すこと。

